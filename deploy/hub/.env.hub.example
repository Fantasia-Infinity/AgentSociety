# Pointer: the canonical Hub configuration example lives at the repository
# root. Copy it into this directory (or use it directly) as `.env.hub`:
#   cp ../../.env.hub.example .env.hub
# The Compose stack reads `.env.hub` from this directory.
